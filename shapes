from manim import *

class Shapes(Scene):
	def construct(self):
		circle = Circle()
		square = Square()
		triangle = Triangle()
		circle.shift(RIGHT)
		square.shift(UP)
		triangle.shift(LEFT)

		self.add(circle,triangle,square)
		self.wait(1)

